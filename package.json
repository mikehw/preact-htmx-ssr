{
  "name": "preact-htmx-ssr",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "build": "swc src -d lib",
    "start": "node lib/index.js",
    "dev": "nodemon --exec \"npm run build && npm run start\" --watch src --ext js,jsx",
    "fmt": "prettier -w src"
  },
  "author": "Mike White",
  "license": "MIT",
  "dependencies": {
    "fastify": "^4.24.3",
    "preact": "^10.18.1",
    "preact-render-to-string": "^6.2.2"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.62",
    "@swc/core": "^1.3.95",
    "nodemon": "^3.0.1",
    "prettier": "^3.0.3"
  }
}
